# Velocity Estimate — {{feature_name}} (backing detail)

**Date:** {{date}}
**Mode:** {{A or B}} — {{units given / units inferred}} {{note if unit list came from a PM-authored PRD table rather than an engineering spec/Jira}}
**Calibration version:** {{calibration.yaml version}}
**Overall confidence:** {{low/medium/high}}
**Shipping strategy found in PRD:** {{yes — using its slices / no — falling back to milestone grouping / no — flat, small feature}}

**Inputs:** PRD `{{path}}` · Inception `{{path}}` · Spec `{{path or "none — Mode B"}}` · Repos inspected: {{list or "none — non-inspectable"}}

> **This is the backing evidence file.** The primary deliverable — labor total, critical-path floor, realistic staffed timeline, per-slice table, and velocity-improvement recommendations — is `{{date}}_{{feature-slug}}_director-brief.md`. Read that first; come here for the per-unit traceability.

---

## Labor total (total effort — NOT a timeline; see the director brief for the timeline)

**{{p50_total}}–{{p75_total}} person-days** (**{{p50_weeks}}–{{p75_weeks}} weeks** at {{working_days_per_week}} days/week, if one person did all of it serially)
*This is summed labor across every unit + expected defect ticket — a budget/headcount input, not a delivery date. Do not read the weeks figure as wall-clock.*

| Component | p50 (days) | p75 (days) |
|---|---|---|
| Units (§1) | {{unit_p50_sum}} | {{unit_p75_sum}} |
| Expected defect tickets (§2) — only if at/below volume ceiling | {{defect_p50_sum or "not summed — see §2"}} | {{defect_p75_sum or "not summed — see §2"}} |
| **Labor total** | **{{total_p50}}** | **{{total_p75}}** |

Reported separately (not in labor total):

| | p50 (days) | p75 (days) |
|---|---|---|
| Mini-inception risk (§3) | {{mi_p50}} | {{mi_p75}} |
| Pre-dev latency | {{pre_dev_p50}} | {{pre_dev_p75}} |
| Integration/release latency | {{int_p50}} | {{int_p75}} |

**Biggest driver(s) of the range:** {{1-2 sentence callout — name the unit(s) or defect-ratio tier driving the spread}}
**Biggest uncertainty:** {{1 sentence — e.g. "Mode B unit list is inferred," "no repo access to confirm foundation vs increment," "surface-type tier assumed typical," "dependency data is inferred, not explicit — critical path in the director brief is directional"}}

---

## §1 — Units

| ID | Name | Slice | Systems | Regime | Archetype | Scope | Depends On | Modifiers | p50 | p75 | Confidence | Drivers |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| U1 | {{name}} | {{slice # or "n/a"}} | {{systems}} | {{ai_leveraged/human_native}} | {{archetype}} | {{small/medium/large}} | {{unit IDs or "—"}} | {{list or none}} | {{d}} | {{d}} | {{conf}} | {{one phrase}} |

**Unit sum:** p50 {{Σ}} · p75 {{Σ}}

{{If Mode B: "Unit list is INFERRED from PRD + inception, not from an enumerated spec/Jira child list. Treat as a prediction, not a plan."}}

{{If dependency data is inferred rather than explicit: "Depends On edges are INFERRED from milestone/slice sequence, not explicit spec/Jira links — critical-path numbers in the director brief are directional."}}

{{Flag any human_native (Mobile) units explicitly here: "OUT OF MODEL — flat low-confidence base_days, do not treat as precise."}}

---

## §2 — Expected defect tickets

**Surface-type tier:** {{low / typical / high}} — {{one-line justification, e.g. "public pre-login page → high"}}
**Ratio applied:** {{ratio}} defects per unit × {{unit_count}} units = **{{expected_defect_count}} expected defect tickets**
**Volume ceiling check:** {{expected_defect_count}} vs. `volume_ceiling_before_flag` ({{ceiling, currently 15}}) → {{"at/below ceiling — summed into labor" OR "ABOVE ceiling — reported as a workload/volume flag only, NOT summed into labor (see calibration.yaml volume_ceiling_note)"}}

**If at/below ceiling:**
**Per-ticket cost:** p50 {{defect_ticket.p50_days}}d · p75 {{defect_ticket.p75_days}}d (calibration.yaml `defect_ticket`)
**Defect sum:** p50 {{expected_defect_count × p50}} · p75 {{expected_defect_count × p75}}

**If above ceiling:**
{{expected_defect_count}} expected tickets is a workload/volume signal for QA and support-capacity planning, not a days-additive line — summing per-ticket elapsed-day medians (which bake in concurrent queue/triage wait, not labor effort) across a volume this far past the evidence base's largest observed case (11 defects, one feature) produces an unvalidated number. See the director brief's staffing section for how this volume compares to the team's recent monthly defect throughput.

*Either way: this is expected, known-cost QA/bugfix work generated by shipping the units above — not the same thing as mini-inception risk below.*

---

## §3 — Mini-inception forecast (separate from labor)

**Signals scored:** {{list which of the 4 signals fired, from the inception/PRD}}
**Risk:** {{low/medium/high}} → **expected_count:** {{n}} additional units
**Added cost:** {{n}} × increment_single_system p50 ({{d}}) + respec_latency_days ({{d}}) = **{{total}} days**

*Re-spec/scope-creep risk from unresolved unknowns at inception time — a schedule-risk contingency, not a certainty.*

---

## Context check (informational — mandatory presentation gate per calibration.yaml's presentation_rule)

Team's trailing 4-week throughput as of {{calibration date}}: {{X}} tickets/4wk ({{defects}} defects + {{features}} features). This estimate implies **{{labor_p50}} person-days ≈ {{% or ratio}}** of that recent output — {{one sentence: plausible / would require multi-week team monopoly, in which case lead with the director brief's per-slice framing instead of this flat figure}}.

---

## Appendix — inputs & assumptions log

- {{Any input file not found, any inference made, any repo not inspected}}
- {{Any calibration number that had to be guessed rather than pulled from calibration.yaml — flag and lower confidence accordingly}}
- {{Any correction made relative to a prior pass on this same feature, and why}}
